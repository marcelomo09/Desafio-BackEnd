version: '3.4'

services:
  api:
    image: myapirest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: